---
import {Image} from 'astro:assets';
const {image, title, describ, link } = Astro.props;


---

<div class="card bg-base-100 w-86 shadow-md hover:shadow-xl">
  <figure>
    <Image src={image} alt={title} />
  </figure>
  <div class="card-body">
    <h2 class="card-title mx-auto">{title}</h2>
    <p>{ describ}</p>
    <div class="card-actions justify-end">
      <a class="btn btn-primary" href={link}>Détails</a>
    </div>
  </div>
</div>